describe('index.js', function (portfolio) {
    // there's stuff in here, too
  });
  

  it('is set as portfolio', function () {
    // tests are here
  });
  let companyName = 'portfolio';
    let mostProfitableNeighborhood = 'Chelsea';it('is set as portfolio', function () {
      expect(companyName).to.equal('portfolio');
    });
    it('is set as portfolio', function () {
      expect(portfolio).to.equal('portfolio');
    });
    let companyName = 'portfolio';

      
